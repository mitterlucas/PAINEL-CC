# Segredos e exports de dados brutos.
# Regra: JSON é bloqueado por padrão; só configs conhecidas do projeto e o
# dado já normalizado (src/data/rnc.json, gerado por scripts/importar-rnc.ts,
# não é segredo) passam.
*.json
!package.json
!package-lock.json
!tsconfig.json
!components.json
!vercel.json
!src/data/rnc.json

# Variáveis de ambiente
.env
.env.*
!.env.example

# Dependências / build
node_modules/
.next/
out/
build/
.netlify/
*.tsbuildinfo
next-env.d.ts

# testes / coverage
/coverage

# diversos
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# vercel
.vercel
